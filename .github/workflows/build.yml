# Deprecated: use .github/workflows/ci.yml instead. Kept for backward compat (release.yml workflow_call).
name: Build (deprecated)

on:
  workflow_call:
    inputs:
      run_tests:
        description: "Run server tests (Go)"
        required: false
        default: true
        type: boolean

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    with:
      run_tests: ${{ inputs.run_tests }}
