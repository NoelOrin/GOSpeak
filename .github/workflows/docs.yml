# GOSpeak Docs — VitePress 构建与部署
#
# 构建文档站并可选部署到 GitHub Pages。
#
# 触发:
#   - push 到 dev/master 且含 docs 变更
#   - PR 到 dev/master 且含 docs 变更
#   - release 发布 (published / edited)
#   - workflow_dispatch 手动

name: Docs

on:
  push:
    branches: [dev, release, feature/**, docs/**]
    paths:
      - "app/docs/**"
      - "packages/sfu-client/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/docs.yml"
  pull_request:
    branches: [release]
    paths:
      - "app/docs/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/docs.yml"
  release:
    types: [published, edited]
  workflow_dispatch:
    inputs:
      deploy:
        description: "Deploy to GitHub Pages"
        required: false
        default: false
        type: boolean

# GitHub Pages 需要这些权限
permissions:
  contents: read
  pages: write
  id-token: write

# 同一时间只允许一个部署
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── 构建 ──────────────────────────────────────────────────────────────
  build:
    name: Build Docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4
        # version 由 package.json 的 packageManager 字段自动获取

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - name: Cache pnpm store
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v5
        with:
          path: |
            ${{ steps.pnpm-cache.outputs.STORE_PATH }}
            node_modules
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build VitePress
        env:
          NODE_ENV: production
        run: pnpm --filter @gospeak/docs build

      - name: Upload static site
        uses: actions/upload-pages-artifact@v4
        with:
          path: app/docs/.vitepress/dist

  # ── 部署到 GitHub Pages ──────────────────────────────────────────────
  # push / PR / release 自动部署，手动触发需勾选 deploy
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: build
    if: |
      github.event_name != 'workflow_dispatch' ||
      (github.event_name == 'workflow_dispatch' && inputs.deploy)
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
