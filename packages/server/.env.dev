DB_TYPE="SQLite"
# SQLite / PostgresSQL / MYSQL
DB_HOST=""
DB_PORT=""

DB_USER=""
DB_PASSWORD=""

DB_PATH=""
# SQLITE

# Redis (optional — leave REDIS_HOST empty to skip Redis connection)
REDIS_HOST=""
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# JWT signing key rotation interval (requires Redis). Examples: 24h, 168h (7d).
# When the TTL expires a new random key is generated and all existing tokens are invalidated.
# Ignored when Redis is not connected (static JWT_KEY is used instead).
JWT_KEY_TTL="24h"
