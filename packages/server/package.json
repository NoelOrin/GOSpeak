{
  "name": "@go-rtc/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "go install github.com/air-verse/air@latest",
    "dev": "$(go env GOPATH)/bin/air",
    "prod": "go run main.go server -e prod",
    "build": "go build -o gospeak-server main.go",
    "test": "node test/run.js"
  }
}
