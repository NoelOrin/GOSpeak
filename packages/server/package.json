{
  "name": "@go-rtc/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "go run main.go server -e dev",
    "prod": "go run main.go server -e prod",
    "build": "go build -o gospeak-server main.go",
    "test": "node test/run.js"
  }
}
